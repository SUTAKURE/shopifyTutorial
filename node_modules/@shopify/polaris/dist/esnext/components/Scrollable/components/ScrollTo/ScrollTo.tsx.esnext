import React__default, { useRef, useContext, useEffect } from 'react';
import { useUniqueId } from '../../../../utilities/unique-id/hooks.ts.esnext';
import { ScrollableContext } from '../../context.ts.esnext';

function ScrollTo() {
  const anchorNode = useRef(null);
  const scrollToPosition = useContext(ScrollableContext);
  useEffect(() => {
    if (!scrollToPosition || !anchorNode.current) {
      return;
    }

    scrollToPosition(anchorNode.current.offsetTop);
  }, [scrollToPosition]);
  const id = useUniqueId(`ScrollTo`); // eslint-disable-next-line jsx-a11y/anchor-is-valid

  return /*#__PURE__*/React__default.createElement("a", {
    id: id,
    ref: anchorNode
  });
}

export { ScrollTo };
