import React__default from 'react';
import { classNames, variationName } from '../../utilities/css.ts.esnext';
import styles from './SkeletonThumbnail.scss.esnext';

function SkeletonThumbnail({
  size = 'medium'
}) {
  const className = classNames(styles.SkeletonThumbnail, size && styles[variationName('size', size)]);
  return /*#__PURE__*/React__default.createElement("div", {
    className: className
  });
}

export { SkeletonThumbnail };
