import React__default, { useEffect } from 'react';
import { useFrame } from '../../utilities/frame/hooks.ts.esnext';

const Loading = /*#__PURE__*/React__default.memo(function Loading() {
  const {
    startLoading,
    stopLoading
  } = useFrame();
  useEffect(() => {
    startLoading();
    return () => {
      stopLoading();
    };
  }, [startLoading, stopLoading]);
  return null;
});

export { Loading };
