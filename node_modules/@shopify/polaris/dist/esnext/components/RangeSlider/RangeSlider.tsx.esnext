import React__default from 'react';
import { useUniqueId } from '../../utilities/unique-id/hooks.ts.esnext';
import { DualThumb as DualThumb$1 } from './components/DualThumb/DualThumb.tsx.esnext';
import { SingleThumb as SingleThumb$1 } from './components/SingleThumb/SingleThumb.tsx.esnext';

function RangeSlider({
  min = 0,
  max = 100,
  step = 1,
  value,
  ...rest
}) {
  const id = useUniqueId('RangeSlider');
  const sharedProps = {
    id,
    min,
    max,
    step,
    ...rest
  };
  return isDualThumb(value) ? /*#__PURE__*/React__default.createElement(DualThumb$1, Object.assign({
    value: value
  }, sharedProps)) : /*#__PURE__*/React__default.createElement(SingleThumb$1, Object.assign({
    value: value
  }, sharedProps));
}

function isDualThumb(value) {
  return Array.isArray(value);
}

export { RangeSlider };
